<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="author" content="{{ meta.author }}">
    <meta name="description" content="{{ description }}">
    <title>{{ title }} - {{ meta.siteName }}</title>
    <link rel="stylesheet" href="{{ meta.root }}/styles/styles.css">
</head>
<body>
    {% block header %}
    <header class="header">
        <a href="{{ meta.root }}/">
            <img class="logo" src="{{ meta.root }}/images/logo.svg" alt="lea11y logo, to start">
        </a>
    </header>
    {% endblock %}
    {% block main %}
    <div class="sidebar-layout">
        {% include "sidebar.njk" %}
        <main class="wrapper">
            <h1>{{ title }}</h1>
            {% include "review-note.njk" %}
            {{ content | safe }}
        </main>
    </div>
    {% endblock %}

    {% include "footer.njk" %}
</body>
</html>
